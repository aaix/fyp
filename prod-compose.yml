
networks:
  shared-network:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.31.0.0/16


x-scylla-base: &scylla-base
  image: europe-west2-docker.pkg.dev/fyp-25coc251/az7-backend/database:latest
  depends_on:
    - scylla-seed
  stop_grace_period: 120s


services:
  jaeger:
    networks:
      shared-network:
        ipv4_address: 172.31.0.254
    image: jaegertracing/all-in-one
    environment:
      - COLLECTOR_OTLP_ENABLED=true
    ports:
      - 8080:16686
      - 4317:4317

  gc-0:
    networks:
      - shared-network
    image: europe-west2-docker.pkg.dev/fyp-25coc251/az7-backend/garbagecollector:latest
    environment:
      BUCKETS: 0-0
      OTEL_URI: "http://jaeger:4317"
      VALKEY_URI: discoverystore-0:6379

      S3_PUBLIC_BUCKET: az7-public
      S3_PRIVATE_BUCKET: az7-private
      S3_ENDPOINT_URL: https://f2254f3b9e16dcc38a23d6ffc5db10a8.r2.cloudflarestorage.com
      S3_ACCESS_KEY_ID: ${SECRET_S3_ACCESS_KEY_ID}
      S3_ACCESS_KEY_SECRET: ${SECRET_S3_ACCESS_KEY_SECRET}
    depends_on:
      scylla-seed:
        condition: service_healthy
      discoverystore-0:
        condition: service_healthy

  watchdog:
    networks:
      - shared-network
    image: europe-west2-docker.pkg.dev/fyp-25coc251/az7-backend/watchdog:latest
    environment:
      OTEL_URI: "http://jaeger:4317"
      VALKEY_URI: discoverystore-0:6379

    depends_on:
      discoverystore-0:
        condition: service_healthy

  discoverystore-0:
    networks:
      - shared-network
    image: europe-west2-docker.pkg.dev/fyp-25coc251/az7-backend/discoverystore:latest


  dataservices:
    networks:
      - shared-network
    image: europe-west2-docker.pkg.dev/fyp-25coc251/az7-backend/dataservices:latest
    environment:
      SCYLLA_URI: 172.31.0.199
      OTEL_EXPORTER_OTLP_TRACES_ENDPOINT: http://172.31.0.254:4317
      VALKEY_URI: redis://discoverystore-0:6379
      ELASTICSEARCH_URI: http://elasticsearch:9200
    depends_on:
      scylla-seed:
        condition: service_healthy
      discoverystore-0:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy

  amplifier:
    deploy:
      mode: replicated
      replicas: ${AMPLIFIER_REPLICAS:-1}

    networks:
      - shared-network
    image: europe-west2-docker.pkg.dev/fyp-25coc251/az7-backend/amplifier:latest
    environment:
      OTEL_EXPORTER_OTLP_TRACES_ENDPOINT: http://jaeger:4317
      VALKEY_URI: redis://discoverystore-0:6379

    depends_on:
      discoverystore-0:
        condition: service_healthy

  gateway-0:
    networks:
      - shared-network
    image: europe-west2-docker.pkg.dev/fyp-25coc251/az7-backend/gateway:latest
    environment:
        DEPLOYMENT_MODE: prod
        OTEL_URI: "http://jaeger:4317"
        VALKEY_URI: discoverystore-0:6379

    depends_on:
      discoverystore-0:
        condition: service_healthy
      dataservices:
        condition: service_started
    ports:
      - 8081:8081

  elasticsearch:
    networks:
      - shared-network
    image: docker.elastic.co/elasticsearch/elasticsearch:8.15.3
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    healthcheck:
      test: ["CMD-SHELL", "curl -s -f http://127.0.0.1:9200/_cluster/health?wait_for_status=yellow&timeout=5s || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 12
      start_period: 40s
    volumes:
      - type: bind
        source: /db/elasticsearch/
        target: /var/lib/elasticsearch

  api-0:
    networks:
      - shared-network
    image: europe-west2-docker.pkg.dev/fyp-25coc251/az7-backend/api:latest
    environment:
      DEPLOYMENT_MODE: prod
      OTEL_URI: "http://jaeger:4317"
      API_SESSION_SIGNING_KEY: ${SECRET_KEY_API_SESSION_SIGN}
      API_SESSION_ENCRYPTION_KEY: ${SECRET_KEY_API_SESSION_ENCRYPT}
      VALKEY_URI: discoverystore-0:6379
      MEDIASERVICES_URI: mediaservices-7yrjjcng3q-nw.a.run.app:443

      S3_PUBLIC_BUCKET: az7-public
      S3_PRIVATE_BUCKET: az7-private
      S3_ENDPOINT_URL: https://f2254f3b9e16dcc38a23d6ffc5db10a8.r2.cloudflarestorage.com
      S3_ACCESS_KEY_ID: ${SECRET_S3_ACCESS_KEY_ID}
      S3_ACCESS_KEY_SECRET: ${SECRET_S3_ACCESS_KEY_SECRET}
      ELASTICSEARCH_URI: http://elasticsearch:9200

    ports:
      - "80:80"
    depends_on:
      discoverystore-0:
        condition: service_healthy
      dataservices:
        condition: service_started
      elasticsearch:
        condition: service_healthy


  scylla-seed:
    networks:
      shared-network:
        ipv4_address: 172.31.0.199
      
    image: europe-west2-docker.pkg.dev/fyp-25coc251/az7-backend/database:latest
    volumes:
      - type: bind
        source: /db/scylla0/
        target: /var/lib/scylla
      - type: bind
        source: ./database/main.yaml
        target: /etc/scylla/scylla.yaml
    hostname: scylla-seed

  scylla-node1:
    <<: *scylla-base
    volumes:
      - type: bind
        source: /db/scylla1/
        target: /var/lib/scylla
      - type: bind
        source: ./database/main.yaml
        target: /etc/scylla/scylla.yaml
    networks:
      shared-network:
          ipv4_address: 172.31.0.200