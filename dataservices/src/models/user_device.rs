// Generated by model_generator.py
#![allow(nonstandard_style)]
use scylla::DeserializeRow;
use uuid;
use scylla::value;
#[derive(Debug, DeserializeRow)]
pub struct UserDevice {
    pub user_id: uuid::Uuid,
    pub device_id: value::CqlTimeuuid,
    pub device_name: String,
    pub device_public_key: Vec<u8>,
    pub encypted_account_key: Vec<u8>,
}
impl UserDevice {
    pub fn consume(self) -> (
        uuid::Uuid,
        value::CqlTimeuuid,
        String,
        Vec<u8>,
        Vec<u8>,
    ) {
        (
            self.user_id,
            self.device_id,
            self.device_name,
            self.device_public_key,
            self.encypted_account_key,
        )
    }
}